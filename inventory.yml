all:
  children:
    bigip:
      hosts:
        bigip1:
          ansible_host: 192.168.86.154

  vars:
    admin_user: admin
    admin_password: 1Pringles!

hosts:
  localhost:
   vars:
     ansible_connection: local
